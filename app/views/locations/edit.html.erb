<% content_for :title, "Edit #{@location.name}" %>
<% breadcrumbs(["Home", root_path], ["Locations", locations_path], [@location.name, location_path(@location)], ["Edit", nil]) %>

<div class="max-w-2xl">
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">Edit Location</h1>
  <%= render "form", location: @location %>
</div>
