<% content_for :title, "New Player" %>
<% breadcrumbs(["Home", root_path], ["Players", players_path], ["New Player", nil]) %>

<div class="max-w-2xl">
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">New Player</h1>
  <%= render "form", player: @player %>
</div>
