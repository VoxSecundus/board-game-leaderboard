<% content_for :title, "Edit #{@player.name}" %>
<% breadcrumbs(["Home", root_path], ["Players", players_path], [@player.name, player_path(@player)], ["Edit", nil]) %>

<div class="max-w-2xl">
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">Edit Player</h1>
  <%= render "form", player: @player %>
</div>
