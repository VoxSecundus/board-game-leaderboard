<% content_for :title, "Bulk Record Plays" %>
<% breadcrumbs(["Home", root_path], ["Plays", plays_path], ["Bulk Record", nil]) %>

<div class="max-w-4xl">
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">Bulk Record Plays</h1>
  <%= render "bulk_form", games: @games, locations: @locations, players: @players, submitted_plays: @submitted_plays %>
</div>
