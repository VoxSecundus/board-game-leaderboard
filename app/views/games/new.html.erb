<% content_for :title, "New Game" %>
<% breadcrumbs(["Home", root_path], ["Games", games_path], ["New Game", nil]) %>

<div class="max-w-2xl">
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">New Game</h1>
  <%= render "form", game: @game %>
</div>
