<% content_for :title, "Edit #{@game.name}" %>
<% breadcrumbs(["Home", root_path], ["Games", games_path], [@game.name, game_path(@game)], ["Edit", nil]) %>

<div class="max-w-2xl">
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">Edit Game</h1>
  <%= render "form", game: @game %>
</div>
